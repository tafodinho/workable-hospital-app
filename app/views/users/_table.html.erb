<div class="relative overflow-x-auto shadow-md sm:rounded-lg">
	<table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
		<thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
			<tr>
				<%# <th scope="col" class="p-4">
					<div class="flex items-center">
						<input id="checkbox-all-search" type="checkbox" class="w-4 h-4 text-blue-600 bg-gray-100 border-gray-300 rounded focus:ring-blue-500 dark:focus:ring-blue-600 dark:ring-offset-gray-800 dark:focus:ring-offset-gray-800 focus:ring-2 dark:bg-gray-700 dark:border-gray-600">
						<label for="checkbox-all-search" class="sr-only">checkbox</label>
					</div>
				</th> %>
				<th scope="col" class="px-6 py-3">
					ID
				</th>
				<th scope="col" class="px-6 py-3">
					User Name
				</th>
				<th scope="col" class="px-6 py-3">
					Email
				</th>
				<th scope="col" class="px-6 py-3">
					Role
				</th>
				<% if can? :manage, User %>
					<th scope="col" class="px-6 py-3">
						Action
					</th>
				<% end %>
			</tr>
		</thead>
		<tbody>
			<% @users.each do |user| %>
				<tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600">
					<td class="px-6 p-4 ">
						<%= user.id %>
					</td>
					<th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
						<%= link_to user.name, user, class: "ml-2 inline-block font-medium" %>
					</th>
					<td class="px-6 py-4">
						<%= user.email %>
					</td>
					<td class="px-6 py-4">
						<%= user.role %>
					</td>
					<% if can? :manage, User %>
						<td class="flex items-center px-6 py-4">
							<%# <button data-modal-target="edit-crud-modal" data-modal-toggle="edit-crud-modal" type="button" class="font-medium text-blue-600 dark:text-blue-500 hover:underline">Show</button> %>
							<%= button_to "Show", user, method: :get, class: "font-medium text-blue-600 dark:text-blue-500 hover:underline" %>
							<%= button_to "Remove", user, method: :delete, class: "font-medium text-red-600 dark:text-red-500 hover:underline ms-3" %>
						</td>
					<% end %>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>
